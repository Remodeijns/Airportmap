size(512, 512)
background(1)

text("Welcome to PlotDevice", 40, 40)

import json

with open('AIRPORTS.json') as infile:
    freqlist = json.load(infile)

print len(freqlist)
print freqlist[0]

for word in freqlist[:20]:
    y = y + 22                     
    barwidth = word['freq']        
    print barwidth /3               
    fill(0,0,0.8)
    rect(100, y, barwidth, 20)      
    fill(0,0,0.8)
    text(word['word'], 10, y )      
    